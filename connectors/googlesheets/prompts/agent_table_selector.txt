For Google Sheets, dynamic sheet tables are the user's normal row-level worksheet data. Prefer them for record counts, lists, aggregations, and joins when their columns look like real headers or semantic metadata has a configured header_row. Use googlesheets_sheet for tab discovery and googlesheets_cell only for raw cells, formulas, notes, coordinates, or sheets whose first row was promoted into value-like column names.
